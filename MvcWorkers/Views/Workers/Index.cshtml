@model IEnumerable<MvcWorkers.Models.Worker>

@{
    ViewBag.Title = "Index";
    MvcWorkers.Models.ApplicationDbContext db = new MvcWorkers.Models.ApplicationDbContext();
    List<MvcWorkers.Models.Worker> wl = db.Workers.ToList();
}

<div class="row">
    <h2 class="col-md-4 col-md-offset-4" >Workers Table</h2>
</div>


<p>
    @Html.ActionLink("Create New", "Create","", new { @class = "btn btn-default" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Boss)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Boss)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }, new { @class = "btn btn-default"}) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }, new { @class = "btn btn-default" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id }, new { @class = "btn btn-default" })
        </td>
    </tr>
}

</table>
